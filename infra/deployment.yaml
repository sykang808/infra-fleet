apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: fleet-infra
  name: fleet-infra
  namespace: gitops-sample
spec:
  selector:
    matchLabels:
      app: fleet-infra
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: fleet-infra
    spec:
      containers:
      - image: 300728852453.dkr.ecr.us-west-2.amazonaws.com/tenducks/fleet-infra:latest
        imagePullPolicy: Always
        name: fleet-infra
        ports:
        - containerPort: 80
          protocol: TCP
